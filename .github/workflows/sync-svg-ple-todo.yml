# NOTE: This workflow needs rework. The infra/ directory was removed during repo cleanup.
# The scripts and docs it references (infra/docs/svg-ple-todo.md,
# infra/scripts/auto-sync-svg-ple-github.sh) no longer exist.

name: Sync SVG-PLE To-Do with GitHub Projects

on:
  workflow_dispatch:
    # Allow manual trigger from GitHub UI

jobs:
  sync-todo:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      contents: read
      projects: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Notice
        run: |
          echo "This workflow needs rework."
          echo "The infra/ directory was removed during repo cleanup."
          echo "infra/docs/svg-ple-todo.md and infra/scripts/auto-sync-svg-ple-github.sh no longer exist."
          echo "Skipping sync."
