═══════════════════════════════════════════════════════════
  🔧 GRAFANA LOGIN ISSUE - SOLUTION
═══════════════════════════════════════════════════════════

Problem: Cannot login to http://localhost:3000/login
Credentials tried: admin / changeme (not working)

═══════════════════════════════════════════════════════════
  🎯 QUICK FIX (30 SECONDS)
═══════════════════════════════════════════════════════════

OPTION 1: Windows Users
───────────────────────
Double-click this file:
  RESET_GRAFANA_PASSWORD.bat

Then login with:
  URL: http://localhost:3000
  Username: admin
  Password: changeme

OPTION 2: Linux/macOS Users
────────────────────────────
Run this command in your terminal:
  ./reset-grafana-password.sh

Then login with:
  URL: http://localhost:3000
  Username: admin
  Password: changeme

OPTION 3: Manual Command
────────────────────────
Open PowerShell/Terminal and run:
  docker exec -it barton-outreach-grafana grafana-cli admin reset-admin-password changeme

Then login with:
  URL: http://localhost:3000
  Username: admin
  Password: changeme

═══════════════════════════════════════════════════════════
  🔍 WHY THIS HAPPENED
═══════════════════════════════════════════════════════════

The .env file with GRAFANA_ADMIN_PASSWORD=changeme was created
AFTER the Grafana container was first started.

When Grafana starts for the first time, it creates a database
with a random admin password. The .env file couldn't override
this because the database was already created.

Solution: Reset the password using Grafana CLI (above).

═══════════════════════════════════════════════════════════
  📖 DETAILED TROUBLESHOOTING
═══════════════════════════════════════════════════════════

See: GRAFANA_LOGIN_TROUBLESHOOTING.md

That file contains:
  • 4 different solution methods
  • Diagnostic commands
  • Log checking procedures
  • Manual database reset (advanced)
  • Common issues and fixes

═══════════════════════════════════════════════════════════
  ✅ AFTER SUCCESSFUL LOGIN
═══════════════════════════════════════════════════════════

1. Grafana will prompt you to change the default password
   → Set a new secure password

2. Verify your dashboard works:
   → Go to: http://localhost:3000/d/barton-outreach-overview
   → Should see 9 panels with data

3. Check Neon datasource connection:
   → Menu → Connections → Data sources → Neon PostgreSQL
   → Click "Save & test"
   → Should see: "Database Connection OK"

4. Import SVG-PLE dashboard (optional):
   → Menu → Dashboards → Import
   → Upload: infra/grafana/svg-ple-dashboard.json
   → Requires BIT migration first

═══════════════════════════════════════════════════════════
  🚨 IF RESET STILL DOESN'T WORK
═══════════════════════════════════════════════════════════

Try recreating the container (will lose any custom changes):

  docker compose down
  docker volume rm barton-outreach-core_grafana-storage
  docker compose up -d

Then login with: admin / changeme

═══════════════════════════════════════════════════════════
  📞 QUICK DIAGNOSTIC COMMANDS
═══════════════════════════════════════════════════════════

Check if Grafana is running:
  docker ps | grep grafana

Check Grafana logs:
  docker compose logs grafana --tail=50

Restart Grafana:
  docker compose restart grafana

Test Grafana API:
  curl http://localhost:3000/api/health

═══════════════════════════════════════════════════════════

CREATED: 2025-11-06
STATUS: Ready to use
