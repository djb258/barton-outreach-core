# Archive

Files moved here during v2.8.0 housekeeping cleanup (2026-02-15).

Everything is preserved in git history. Recovery tag: `pre-cleanup-20260215`

## Contents
- `scripts-oneoff/` — One-off diagnostic/fix Python scripts (from repo root and scripts/)
- `reports/` — One-time audit reports, certification docs, status reports (from repo root)
- `data-files/` — CSVs, logs, SQL dumps, JSON exports (from repo root)
- `legacy-folders/` — Deprecated/redundant directory structures
